前言:建议修改代码使用 Notepad++ 或 Dreamweaver cs6 进行修改代码 

通过上述代码修改工具打开二次解析文件夹中 index.php 文件

1.将index.php中下方代码进行替换

<?php include ('./data/beipy.inc.php');

?>

替换为

<?php include ('./data/beipy.inc.php');
define('REFERER_URL', 'baidu.com|qq.com');//如：define('REFERER_URL', www.test.com|m.test.com|api.test.com'); 即可
	$uriarr = parse_url($_SERVER['HTTP_REFERER']);
	$host = $uriarr['host'];
	$ymarr = explode("|",REFERER_URL);
    if(in_array($host,$ymarr)){
?>

PS:替换内容中 baidu.com|qq.com 为你要授权的域名，更换为你要授权的域名 可添加N多个授权域名 每个域名中间要以|为分隔  类似于 xxx.com|xxx.cn|xxx.net 以此类推

2.将以下代码放到index.php文件中最底部 </html>下方

<?php }else{echo "视频解析受限";}?>

PS:代码中 视频解析受限可根据具体需求进行修改。

授权完毕！